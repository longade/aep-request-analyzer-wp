import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    divIcon: {
        display: 'block',
        position: 'absolute',
        top: 0,
        left: 0,
        cursor: 'pointer',
        color: '#fff',
    },
    pIcon: {
        transform: 'translate(150%, 150%)',
        fontWeight: 'bold',
    },
}));
