import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    divIcons: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
        width: '240px',
    },
}));