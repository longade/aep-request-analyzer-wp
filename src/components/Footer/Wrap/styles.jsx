import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    divWrap: {
        padding: '16px 24px',
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        maxWidth: '1300px',
        margin: '0 auto',
    },
}));