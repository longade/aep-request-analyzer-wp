import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    linkLogo: {
        color: '#fff',
        justifySelf: 'start',
        cursor: 'pointer',
        textDecoration: 'none',
        fontSize: '1.5rem',
        display: 'flex',
        alignItems: 'center',
        marginBottom: '16px',
        fontWeight: 'bold',
    },
}));