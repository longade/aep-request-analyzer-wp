import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    containerDiv: {
        /* width: '100vw', */
        minHeight: '100vh',
        padding: '5rem calc((100vw - 1300px) / 2)',
        background: '#150f0f',
        color: '#fff',
    }
}));