import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    h1: {
        fontSize: 'clamp(2rem, 2.5vw, 3rem)',
        textAlign: 'center',
        marginBottom: '5rem',
    }
}));