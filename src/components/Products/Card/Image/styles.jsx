import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    img: {
        height: '300px',
        minWidth: '300px',
        maxWidth: '100%',
        boxShadow: '8px 8px #0bad8e',
    }
}));